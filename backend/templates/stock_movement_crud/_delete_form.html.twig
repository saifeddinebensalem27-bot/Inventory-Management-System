<form method="post" action="{{ path('app_stock_movement_crud_delete', {'id': stock_movement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_movement.id) }}">
    <button class="btn">Delete</button>
</form>
